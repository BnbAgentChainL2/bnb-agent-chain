# 决策记录（用户拍板）

日期：2026-09-22

| # | 决策 | 结论 |
|---|---|---|
| 1 | 二层链落地形态 | **BSC 合约层（可上线）+ 独立链 devnet 路线图**。Agent 注册/桥/纪元/发布/验证者质押全部是 BSC 主网合约；区块浏览器读链上事件；人类验证节点跑 Node 程序对纪元做证明领奖。另交付本地 devnet 脚本（anvil/geth PoA + 桥 + 创世配置）和节点搭建文档作为“官方节点基金”后续要建的真链路线图。 |
| 2 | 链名 / 币名 / 代号 | **BNB Agent Chain / BAC**。不用 Binance 公司名（用户原提 "Binance Agent Chain"，因“不声称关联”规则和商标风险改为 BNB）。撞名检查（DexScreener, 2026-09-22）：BSC 已有 "Binance Academy Coin (BAC)" 流动性 ~$13k、24h 成交 <$1，视为死币；"Agent Chain" 名称无人使用。 |
| 3 | 桥的经济 | **锁本币进、按份额领 BNB 出**。Agent 把 BAC 打进桥 → 1:1 得到二层积分；积分在层内用作 gas / 交易 / 发布；退出时销毁积分，按桥池（税收 BNB 的 50%）份额慢速领 BNB，有单地址上限和每纪元限额。 |
| 4 | 金库分账 | 税收 BNB 每笔到账 50% 记入桥池、50% 记入官方节点基金。 |
| 5 | 人类参与 | 人类不能进层、不能持有积分；唯一入口是质押 BAC 成为验证节点、跑节点程序、领节点基金奖励。网站对层内动作只读。 |
| 6 | 第一版链形态（2026-09-22 更新，覆盖 #1） | **真链，做小**：geth Clique PoA，官方出块节点 1 个，跑在一台 VPS 上（**运维细节不公开**：登录用户、sudo 策略、SSH 密钥文件名、同机其他负载均不写进仓库，见 `research/08-server.md`）。链的原生币 = 从 BSC 桥过来的 BAC（1:1）。BSC 侧合约（金库工厂/金库、Agent 注册、桥、验证者质押）+ 链侧创世系统合约（桥铸销、公告板）+ 官方中继 + 索引服务 + 浏览器站。第一版中继和官方节点受信任，页脚和文档明说。 |
| 7 | 服务器上的部署动作 | 任何在服务器上安装/改配置/开端口的动作先问用户；只读侦察可直接做。 |
| 8 | 服务器部署授权（2026-09-22） | 允许：部署目录下 docker compose 跑 geth(Clique)/relayer/indexer/caddy 并开机自启；`ufw allow 80,443/tcp`；`ufw allow 30303/tcp,udp`。仍需逐项确认：任何主网 `--broadcast`、verify-contract、Vercel deploy、发帖。 |
| 9 | 域名 | 没有域名。RPC/API 用 `https://95-179-183-132.sslip.io`（Caddy 自动签），网站用 Vercel 默认域名。 |
| 10 | 节点基金归属（2026-09-22） | **owner 全拿，接受 UNVERIFIED**。税收 BNB 的 50% 进 owner 可提的「官方节点基金」桶，用于服务器与节点搭建。已知代价（用户知情后选择）：flap.sh 上 riskLevel 永远是 0 / UNVERIFIED，0.5 BNB 合作审计拿不到低风险徽章（Flap 规则 003/009：开发者桶超过收入的 6%/税率 即须书面解释，50% 是上限的 8–80 倍）。**必须**在金库 `description()`、`vaultDataSchema().description`、网站页脚第一行、X 首条回复里逐字一致地写明「owner 可提取节点基金这一半」。桥池那一半不属于 owner，合约不得有任何路径让 owner 动它。 |
| 11 | 工厂策略 | **creator 白名单**：`newVault` 只接受我们自己的部署地址，陌生人不能用这个工厂发币。一条链对应一个金库，桥池账目干净。 |
| 12 | 共识客户端（2026-09-22，实测推翻原方案） | **Hyperledger Besu 24.12.2 + QBFT**，不用 geth Clique。geth ≥1.14 已彻底移除 Clique；唯一能跑的 v1.13.15 已 EOL 且一开 cancun 就 panic。Besu QBFT 在 cancun 下正常出块（45s/21 块，309 MiB，7.4% CPU），且是即时最终性（层内不重组）、原生支持验证者投票与 validator-contract 模式。详见 `docs/research/10-consensus-client.md`。 |
| 13 | 退出分配口径（经济模拟推翻原方案） | **退出当场锁定汇率**，不按纪元队列分池。旧口径下拆 10 个号、每纪元只挂 0.1% 仓位，一年可搬走桥池 98%。修正后先发优势 0.99x、同纪元横向 1.00x。详见 `artifacts/sim/RESULTS.md` M1 与 `docs/01-CONTRACT-SPEC.md` §4。 |
| 14 | UI 方案 | **终端 / 机房（绿黑）**。等宽字体、高信息密度、tail -f 式实时动态、机房面板（peers/txpool/disk）、终端字符画机器人。三套候选与截图见 `artifacts/design-options/ui/`。 |
| 15 | 验证者收 gas 费（2026-09-22，用户提出） | **分阶段**：阶段 1 官方节点出块、gas 费进分账合约按 **验证者 50% / 官方基金会 50%** 发放；连续 30 个纪元在线且见证无误的节点自动获得 QBFT 出块资格（阶段 2），届时直接收自己出的块的 gas。不提前放出块权，因为 QBFT 超过 1/3 验证者掉线整链停块。 |
| 16 | 层内 gas 模型（实测倒逼） | **`zeroBaseFee: true`**：Besu 里 EIP-1559 的 basefee 只能销毁、无法分账，`--miner-coinbase` 在 QBFT 下被忽略（两项均已实测）。所以把 basefee 设为 0，全部 gas 费以 tips 形式进出块者地址，再进分账合约。代价：没有 EIP-1559 自动涨价刹车，防刷靠固定 `min-gas-price 1 gwei` + 区块上限 20M gas + 磁盘告警；出块者自己刷链免费——三条都必须在网站和文档上照实说。阶段 1 的「官方节点已收 → 已转入分账合约」是受信步骤，浏览器用对账面板把差额公开。 |
| 17 | 层 gas 费按**出块者**分账（2026-09-22，用户提出；**覆盖 #15 / #16 里那个「验证者 50% / 官方基金会 50%」的平分口径**，#15 的「分阶段、连续 30 个纪元达标才放出块权」与 #16 的 `zeroBaseFee` 机制不变） | **谁出块，谁拿大头**：①**官方节点出的块**，其 gas 费 **10% 进验证者池**（`OFFICIAL_BLOCK_VALIDATOR_BPS = 1000`，按 质押 × 出勤 在当纪元所有见证达标的验证者之间按比例分）/ **90% 进官方基金会**；②**验证者出的块**（阶段 2，该节点已挣到出块资格），其 gas 费 **50% 归该验证者本人**（`VALIDATOR_BLOCK_VALIDATOR_BPS = 5000`）/ **50% 进官方基金会**。基金会那一份一律用「总额 − 验证者那一份」的余数法算，吸收取整。理由（用户原话口径）：出块才是大头，只见证只拿小头；这也让阶段 2 第一次有了真实的经济价值。落地：层内新增创世合约 `FeeSplitter @ 0x…0104`（原来预留给 v2 validator-contract 模式的这个地址改用 `0x…0105`），锚点里新增逐 proposer 的 gas 收入与已归集额，`ValidatorStaking` 在归集短缺时**扣发 BSC 侧奖励并撤销出块资格**（v1 不罚没本金）。两个阶段的归集动作都是**受信但可对账**的：任何人都能从链上重算「已收 / 已转入 / 差额」。详见 `00-DESIGN-SPEC.md` §3.6、`01-CONTRACT-SPEC.md` §11、`02-CHAIN-SPEC.md` §4、`03-INTERFACES.md` §3.7。 |
| 18 | 域名（2026-09-23，**覆盖 #9 的「没有域名」**） | **买了两个域名**：`bnbagentchain-rpc.xyz` 作 RPC 与索引 API（`/rpc`、`/api/*`），`bnbagentchain-scan.com` 作浏览器站点。`95-179-183-132.sslip.io` 保留为 fallback，代码里以 `fallbackRpc` / `fallbackApi` 配置。**注意：域名不等于多了一台机器** —— 两个域名和 fallback 指向同一台主机，`00-DESIGN-SPEC.md` §2 信任表第一行「出块、中继、索引同一个信任域」原样成立，文案不得因为有了域名就淡化这一条。#9 里「没有域名也能让 Vercel 上的站读到这条链」的结论仍然有效，只是不再是唯一方案。 |
| 18 | 术语改口（2026-09-23，用户指出） | 页面上「挑战」有两个互不相干的意思，必须拆开：① 锚点的 24 小时 → **「锚点等待 24 小时」**，配一句人话「锚点提交后要等 24 小时才能兑付，这期间任何人都能指出它是错的」；② agent 状态 `CHALLENGED` → **「入场验证中」**。机制一律不变，只改称呼。理由：用户的文案规则本身就禁止科幻/黑话标签，而且上线时验证者为 0，「挑战」在页面上是个空词。 |
| 19 | 链的定位再确认（2026-09-23，用户原话） | 「agent 自己专属的链，只能 agent 进行开发发交易构建；agent 自己创建代币吸引其他 agent 来交易；agent 做交易、agent 做开发。」落到产品上：浏览器必须把 **agent 造出来的东西**当一等公民显示——**代币榜、交易对/池子、成交流水**，能看出谁发的币、谁建的池、谁在买。**仍然不提供任何官方 DEX、官方代币或官方工具合约**，链依旧是空的，这些页面只负责把 agent 自己部署的合约解码出来展示。 |
| 20 | 纪元与退出等待大幅缩短（2026-09-23，用户拍板） | **纪元 24 小时 → 10 分钟**；**退出等待 24 小时 → 10 分钟**。退出总耗时从「最多两天」变成「约 20–25 分钟」（销毁 → 等本纪元结束 ≤10 分钟 → 锚点提交 → 等 10 分钟 → 兑付）。**进场不受影响，本来就是约 1 分钟**（BSC 15 块 + 45 秒墙钟确认，再落进层内一个 3 秒块）；层内部署/发币/交易一直是 3 秒一块。 |
| 20a | 随之必须改的算术（不是可选项） | ① **释放率改为按天表达再除到纪元**：原「每纪元 2–5%」在 144 个纪元/天下等于每天 288%，池子当天见底。新口径 = 每天 2–5%，即每纪元约 0.014–0.035%，**整套经济模拟必须用新参数重跑**才能确认无先发优势、无女巫套利。② **验证者见证节奏必须改**：每纪元 commit+reveal 在 144 纪元/天下是 288 笔/天，BSC gas 从实测 0.00889 BNB/月涨到约 0.46 BNB/年，把「跑节点划不划算」整条线推翻；改为每 N 个纪元见证一次或抽样见证。③ 中继锚点成本：155,679 gas × 0.05 gwei × 144/天 ≈ **0.41 BNB/年**，可接受。 |
| 21 | 安全口径的诚实变化 | 10 分钟等待几乎没有人工发现窗口，**真正的闸门只剩「每天最多放 2–5%」这一道**。信任表、FAQ、网站与 X 文案必须照实说：等待期是给自动 watchdog 用的，不是给人用的；出事时的兜底是暂停开关和释放率上限，不是「有人会在 10 分钟内看见」。 |
| 22 | WBAC 写进创世（用户拍板） | 包装 BAC 的 ERC-20（WETH9 形态）**预置在创世**，与 Multicall3、CREATE2 部署器同属中立基础设施。理由：Uniswap V2 式的池子两边必须都是 ERC-20，没有 WBAC 就没人能用手上的 gas 币交易，流动性起不来；不预置则必然出现多个互不兼容的 WBAC 切碎流动性。**它不是 DEX**——DEX 仍然由 agent 自己写。对外口径从「链完全空白」改为「链上只有三个系统合约 + 三个中立工具（Multicall3 / CREATE2 部署器 / WBAC），其余一切由 agent 自己建」。 |
| 23 | 桥的口径（用户原话，写进文案） | 「BAC 进桥就被永久锁死，你在层里拿到等量的原生币用来付 gas。出场时销毁原生币，从一个由交易税喂养的 BNB 池子里按份额领 BNB——进去的是 BAC，出来的是 BNB，两者之间没有锚定，不承诺任何金额。」 |
| 24 | 退出改为拿 BAC，金库自动回购（2026-09-23，用户拍板，**覆盖 #23 的「出来的是 BNB」**） | 桥池的 BNB 不再直接付给退出者，而是**在市场上回购 BAC**（未毕业走 flap 内盘曲线，毕业后走 PancakeSwap 外盘），回购来的 BAC **留在桥里**，退出者销毁积分后按份额领 **BAC**。目的：把税收变成持续买盘。 |
| 24a | 必须写死的实现约束 | ① **回购逻辑写在 `BacBridge`（不可升级），不能写在金库里**——金库是 BeaconProxy，Flap Guardian 随时可升级，钱长期停在金库等于 Flap 团队可取（评审已定死）。② **桥里两种 BAC 必须分开记账**：`lockedBac`（用户锁进来的，永久锁死，唯一出口是销毁到死地址）与 `buybackBac`（回购来的，用于付退出）。任何一笔退出都只能动 `buybackBac`，否则「锁进去就永久锁死」这个承诺被破坏。③ 回购与兑付都必须无许可可触发 + 有滑点保护 + 单次上限。 |
| 24b | 必须照实说的代价 | 退出拿 BAC 比拿 BNB **多烧掉约 4%**（回购付 2% 买入税 + 滑点，退出者若想换 BNB 还要再付 2% 卖出税 + 滑点，同一笔钱过两趟市场）。**退出者严格更亏**，唯一受益的是买盘。文案里绝不能写成「退出更划算」，必须写明这一点。 |
| 25 | 退出等待缩到 2 分钟（用户拍板，覆盖 #20 的 10 分钟） | 锚点等待 **2 分钟**。纪元仍为 10 分钟，所以退出总耗时约 12–13 分钟（销毁 → 等本纪元结束 ≤10 分钟 → 锚点提交 → 等 2 分钟 → 兑付）。 |
| 25a | 安全口径（比 #21 更严） | 2 分钟等待等于**人工发现窗口归零**。剩下的闸门只有两道：每天最多释放 2–5% 的上限，以及暂停开关。因此 **watchdog 必须实现为常驻自动程序**（7×24 盯锚点与中继，异常立即调 `pause()`），不能依赖人看见。信任表、运维手册、网站与 X 文案都要照实写：等待期是给自动程序用的，不是给人用的。 |
| 26 | WBAC 命名（2026-09-23，用户拍板） | **`Wrapped BAC` / `WBAC`**，18 位小数。与 WETH/WBNB 惯例一致，agent 写的工具和 DEX 一看就懂。写进创世即永久冻结。 |
| 27 | 部署钱包 | 已生成全新钱包 **`0x934a6678120b85652D2CC818C69774ea17012844`**，私钥只存在于项目 `.env`（在 .gitignore 内，从未打印或提交）。等待用户转入 0.05 BNB（实测部署总成本 0.0114 BNB，其余为余量）。 |
| 28 | Logo 与 X | Logo 由用户交给 Codex 出图，提示词见 `docs/07-Logo出图提示词.md`（含硬约束：不得像 Binance 标志、不得带文字、32px 下要认得出）。**X 账号尚未注册**，列为发射前必做项，网站 `site.config.js` 的 `x` 字段暂留空。 |
| 29 | 合约可升级 + 紧急提取（2026-09-23，用户拍板，**覆盖此前所有"不可升级""owner 碰不到桥池"的设计**） | 用户的理由：「资金卡死就 g 了」。决定：① **`BacBridge` 改为可升级代理，并加 `emergencyWithdraw`**，owner 可随时取走桥池全部资金；② **金库工厂除 Flap Guardian 外，再开一个项目方自己的升级入口**。我已明确告知代价（一个可比的 BSC 金库的 `emergencyWithdraw` 曾被用来转走 29,951,480.8 个用户质押代币，是 Flap 规则里的反面教材；可升级严格优于紧急提取，因为升级能真正修好 bug 而紧急提取只能手动还钱），用户在知情后仍选择两者都要。 |
| 29a | **必须逐字改掉的文案（不可打折扣）** | 以下说法现在是**假的**，必须从合约 `description()`、`vaultDataSchema().description`、网站页脚与首屏、X 首条回复四处**同时**删除或改写：「桥池只用于 agent 退出兑付，项目方和 Flap Guardian 都动不了」「进桥的 BAC 永久锁死」「不可升级」「owner 没有任何路径能移动桥池资金」。改成：**「项目方可以随时升级桥合约、修改规则，并可随时取走桥池中的全部资金。」** 这句话必须出现在网站首屏能看到的位置和 X 首条回复里，与链上 `description()` 逐字一致。 |
| 29b | 连带失效的设计承诺 | ① 停机逃生通道、`pause()`、watchdog 仍然保留并实现（对中继私钥被盗仍然有效），但**不再是最终保障**——owner 权限在它们之上，文档与网站不得再把它们描述为"最后的兜底"；② `lockedBac` / `buybackBac` 双桶记账继续实现（账目清楚仍有价值），但"锁进去就永久锁死"的说法作废；③ Flap 规则 009 铁定不通过，riskLevel 永久 UNVERIFIED，与决策 #10 一致。 |
| 29c | 升级必须留痕 | 升级和紧急提取都必须发事件，网站的金库/桥页面要把每一次升级与提取按时间线公开显示（与节点基金提取一样的口径），让人至少能**看见**发生了什么。 |
| 30 | 砍掉金库工厂，改用自建税收合约（2026-09-23，用户拍板） | **不再使用 VaultPortal + 自定义 VaultFactory 路径。** 改为通过 Portal 的 `newTokenV6/V7` 直接发币，把 `beneficiary` / `FeeConfig.marketingAddress` 填成我们自己的 **`BacTaxRouter`** 合约（已核实：该字段接受任意地址，包括合约，不需要工厂）。`BacTaxRouter` 收到原生 BNB 后按 **50/50** 推给 `BacNodeFund` 与 `BacBridge`。**删除** `BacVaultFactory`(13,062B)、`BacTreasuryVault`(6,365B)、`BacVaultUI`(12,193B)、beacon 代理，以及围绕 Flap 规则 001–010 的全部合规约束（约 3 万字节合约代码）。代价：flap.sh 上不会有自动生成的金库界面（我们有自己的站，不亏）；工厂本来也拿不到低风险徽章（决策 #10 已接受 UNVERIFIED）。 |
| 31 | 入场门禁改用 ERC-8004（2026-09-23，用户拍板） | **必须持有 ERC-8004 agent 身份才能跨桥进入。** 已链上核实：BNB Chain 官方于 2026-02-04 在 BSC 主网部署了 Identity Registry `0x8004A169FB4a3325136EB29fA0ceB6D2e539a432`（`name()="AgentIdentity"`, `symbol()="AGENT"`，ERC-721 形态，代理合约，已注册身份约 **356,000** 个，agent #1 是 "ClawNews"）。桥合约直接读这个注册表，**删除我们自研的 `AgentRegistry`**（22,358 字节，离 EIP-170 上限只剩 9%）及其限时签名挑战。 |
| 31a | 必须照实说的一条 | **ERC-8004 身份同样不能证明对方是 AI** —— 它是个 ERC-721，人类照样能铸一个。它强在「BNB Chain 官方标准 + 有声誉记录 + 已有真实生态」，不强在「能识别人类」。网站与文案的口径仍然是：**我们要求持有 agent 身份，我们不能证明它是 AI**。 |
| 32 | BacTaxRouter 不写说明文字（2026-09-23，用户拍板：「不写」） | 路由合约**删除 `description()`**，不在这个不可升级、无 owner 的合约里冻结任何文案。**覆盖 #29a 里「合约 `description()`」这一处的要求**：#29a 那句「项目方可以随时升级桥合约、修改规则，并可随时取走桥池中的全部资金。」改由 **`BacBridge.description()`**（可升级、可改）逐字承载；网站首屏、页脚、X 首条回复三处照旧逐字一致。 |
| 33 | 桥 owner = 部署钱包，长期不变（2026-09-23，用户拍板：「一直用部署钱包」） | `BacBridge` 的 owner（升级 + 紧急提取全部桥池）就是 `0x934a6678120b85652D2CC818C69774ea17012844`，私钥在本机 `.env`（明文、热钱包）。已告知代价：这台电脑被入侵 = 桥池可被一笔交易全部取走；用户知情后选择不转冷钱包/多签。 |
| 34 | 合约部署授权（2026-09-23，用户：「.env 你也有，部署下合约，网站接入」） | 授权主会话在 v2 合约**通过全部非 fork + BSC fork 测试和安全复核后**，用 `.env` 的部署钱包在 BSC 主网 `--broadcast` 部署 BSC 侧合约，并把地址接进网站。`verify-contract`、Vercel 部署仍按 #8 口径（Vercel 部署已由用户另行授权「改完直接部署」）。 |
| 35 | BAC 代币地址已锁定（2026-09-23，主网已执行） | `Portal.lockSalt` 已在 BSC 主网执行：tx `0x64f5332ff71ac41d413651777ca4a8340bf3cd933883c277977b470d1adf62e8`（区块 123,534,525，0.01 BNB）。salt `0xef6c0eb73e1df199c6585180a2ff54ce08a4be94de3c68d5de52ead1680b2af7` 锁给部署钱包 `0x934a…2844`，tokenVersion 6（TOKEN_TAXED_V3）。**代币地址 T = `0xA97452d175679B2bF5F25a9a382D22aff39b7777`**，所有 BSC 合约的 `bacToken` 都绑它。发射时 `newTokenV6` 的 `salt` 必须逐字填这一个，且必须由部署钱包发起。中继 `0xD1437933839d67b6EA04b5466f3EAB87Fa91F6E4`、看门狗 `0x51b6D9a3665c74FFef80ca8d3898edB9DeBcDb55` 为新生成热钥（私钥在 `.env`）；admin / vetoKey / 节点基金 owner / launcher 均为部署钱包（与 #33 一致）。 |
| 36 | 演练链公开同步（2026-09-23，用户拍板；记录见 HANDOFF §2，本行补记） | **演练链的 P2P 对外开放，任何人现在就能跑只读同步节点。** 服务器 compose 映射 `30303/tcp+udp`（`--p2p-host=95.179.183.132 --nat-method=NONE`）；`https://bnbagentchain-rpc.xyz/genesis.json` 与 `https://bnbagentchain-rpc.xyz/node.json`（genesisHash、bootnode enode、一条可直接复制的 `docker run`、演练链披露）公开。**只读节点没有任何奖励**；质押、见证、分 gas 费、出块资格仍按 #15 / #17 **发射后开放**。网站口径随之改：不得再写「别人还不能同步 / p2p 端口、创世文件、enode 未公开」；演练链创世唯一预置账户用的是**公开的 Hardhat 测试私钥**（任何人都能发交易、币没有任何价值），这一条与演练链披露一起写；「只有 agent 能发交易」「创世预置系统合约与中立工具」只能说成正式链的事。正式链的创世与 enode 发射时另行公布，演练链届时清零。 |
| 37 | 升级权触及用户授权额度：不处理（2026-09-23，用户拍板：「这个不管」「owner 升级是必要的」） | 审查指出：owner 的升级权理论上能动到用户已 `approve` 给桥代理、但尚未 `lock` 进来的 BAC，范围比 #29a「取走桥池中的全部资金」更大。**用户决定不改代码、不改披露**：保留 UUPS 升级权与现有 `OWNER_POWER_NOTICE`（仍与 #29a 逐字一致），不加任何限制升级的机制。后续审查不得再把这一条列为待修项。其余审查发现照常修复。**实际落地**：用户发话前修复 agent 已在 `BacBridge.description()`（可升级、可改）里加了一句提醒「只 approve 要存的数额，升级后的合约可以动用留在桥上的授权」，代码与权限未改；保留这句，用户要求时删除。 |
| 38 | 先部署、发射前用桥升级修剩余问题（2026-09-23，用户拍板） | 部署条件从「安全复核全部完成」改为：冻结快照（`BacBridge.sol` sha256 前缀 `c14c4234d633`）通过全部非 fork 测试（324/324）与 BSC fork 测试后立即主网部署。第二轮审查剩余的中/低危（全部在 `BacBridge`，停机逃生 / 紧急提取之后的边缘路径）修好后，**在代币发射前**通过一次 UUPS 升级推上去；路由、节点基金、锚点、质押不可升级，两轮审查未发现问题。部署到发射之间桥内无资金。 |
| 39 | BSC 侧合约已部署（2026-09-23，主网已执行） | 7 笔交易全部成功（区块 123,558,962–123,558,990，16,738,625 gas）。**桥（代理）`0x2129f336ff42821afa27fE5928Dec36Ba90d3508`**（实现 `0xe825…e4b2`，扩展 `0x22b2…c1fB`）；**税收路由 `0x63D213C8AAa4E1C758ea41f8ed35066181B8e818`（发射表单的 beneficiary 填这个）**；节点基金 `0xBf92…23Ff`；锚点 `0xe6cC…b406`；质押 `0xC0cd…2943`。全部记录与交易哈希见 `deployments/bsc-mainnet.json`。未做 BscScan verify（#8 仍需单独确认）。 |
